name: Runner check

on:
  push:
    branches: [claude/analytics-modeling-evaluation-dz5iic]
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - run: echo "runner reached"
